<UserControl x:Class="Lurker.UI.Views.UpdateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Image Source="{Binding FilePath}" Height="80" Visibility="{Binding IsNotWorking, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        <Controls:ProgressRing IsActive="{Binding IsWorking}"/>
    </Grid>
</UserControl>
