<UserControl x:Class="Lurker.UI.Views.ItemOverlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Lurker.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="100">
    <Button Height="100" Width="100" x:Name="Close" Background="Green">
        <StackPanel>
            <TextBlock Text="{Binding Rarity}"/>

            <TextBlock Text="{Binding ItemInformation}"/>
            <TextBlock Text="{Binding SocketInformation}"/>
            <!--<TextBlock Text="{Binding BaseType}"/>
            <TextBlock Text="{Binding ItemClass}"/>
            <TextBlock Text="{Binding ItemLevel}"/>-->
        </StackPanel>
    </Button>
</UserControl>
