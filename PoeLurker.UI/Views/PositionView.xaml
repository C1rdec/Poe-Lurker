<UserControl x:Class="PoeLurker.UI.Views.PositionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PoeLurker.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="10,0,0,0" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="120">
        <TextBlock FontFamily="pack://application:,,,/Assets/#Fontin"
                   Foreground="{Binding Foreground}"
                   TextTrimming="CharacterEllipsis"
                   FontSize="20"
                   FontWeight="Bold"
                   Text="{Binding StashName}"/>
        <TextBlock HorizontalAlignment="Left" FontFamily="pack://application:,,,/Assets/#Fontin"
                   TextTrimming="CharacterEllipsis"
                   Foreground="{Binding Foreground}"
                   FontSize="15"
                   Text="{Binding Location}"/>
    </StackPanel>
</UserControl>
